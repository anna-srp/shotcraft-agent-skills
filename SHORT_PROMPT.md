Build, verify, and publicly deploy the English-language, United States Shotcraft Agent using <https://github.com/anna-srp/shotcraft-agent-skills>.

Open or clone the repository, read its `README.md`, `PROMPT.md`, Agent persona, and four Skills, then follow the official `zoowork-managed-agents` guidance. Ask me only to save `ZOOWORK_API_KEY` in the ignored `.env`; never ask for an Agent ID or Skill ID. Run `npm ci && npm run setup` and keep the verification no-render and under two minutes.

After setup passes, do not stop or ask whether I want a UI. Reuse the same Agent and build a lightweight, original Shotcraft web UI with product-video inputs, a streaming production view, durable long-render recovery, artifact download, loading and error states, and New conversation. Keep the API key and Agent ID server-side, create a separate Session for each visitor or conversation, add a basic usage limit, accept only text or authorized public asset URLs in the MVP, and deploy it to Vercel. Make `Autonomous — produce the complete video` and `Produce video` the defaults. A real request to make, create, produce, render, or deliver a video must continue from brief to storyboard, Remotion render, actual-video review, artifact publication, and MP4 delivery without asking again. Keep installation testing no-render; if Vercel needs interactive login, ask only for that authorization and continue.

Finish in chat with the running Agent status, quick-test result, public Vercel URL, UI test result, and any verified upload limitation. If I supplied a real product-video task, do not finish until its reviewed MP4 or a concrete terminal blocker is delivered. Do not substitute a brief or Markdown report for the requested video.

My additional requirements: [add changes here, or leave as none].
