{
  "name": "shotcraft-agent-deployment",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "node scripts/provision.mjs",
    "verify": "node scripts/verify.mjs",
    "setup": "npm run deploy && npm run verify",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@zoowork-ai/sdk": "0.7.0"
  },
  "devDependencies": {
    "vite": "6.4.3"
  }
}
